# Copy to `.env` and fill in. Keys prefixed with EXPO_PUBLIC_ are bundled into
# the client JS — anyone who unpacks the app can extract them. Acceptable for
# personal builds; replace with a backend proxy before any public release.

# Anthropic — https://console.anthropic.com/settings/keys
EXPO_PUBLIC_ANTHROPIC_API_KEY=
